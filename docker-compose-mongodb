version: '3'

services:
    mongodb:
        # build: . //build é usado quando o Dockerfile está no mesmo diretório
        image: mongo:latest
        restart: always
        ports:
        - 27017:27017
